### Placeholder

module s3_backend {
    # source = "../../s3_backend"
    source = "github.com/rpgd60/tf-modules//s3-backend/"

    company = var.company
    project = var.project
    environment = var.environment
}